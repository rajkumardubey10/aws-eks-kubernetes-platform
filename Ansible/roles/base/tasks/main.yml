- name: Update apt cache
  apt:
    update_cache: yes

- name: Install base packages
  apt:
    name:
      - curl
      - wget
      - unzip
      - gnupg
      - software-properties-common
      - ca-certificates
      - lsb-release
      - apt-transport-https
      - git
    state: present